export default function CartPage() {
  return (
    <section className="panel">
      <h1>Cart</h1>
      <p className="muted">Cart UI shell. Connect this page to `/api/cart` with a stored JWT.</p>
    </section>
  );
}
